[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "powershell", "homebrew", "msi"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "aarch64-pc-windows-msvc", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Path that installers should place binaries in
install-path = "~/.local/bin"
# Keep release archive names/formats compatible with direct downloads and cargo-binstall.
unix-archive = ".tar.gz"
# Where to host releases
hosting = "github"
# Publish Homebrew formulae to the Raeva tap after release artifacts are hosted.
tap = "raeva-io/homebrew-tap"
publish-jobs = ["homebrew"]
# Whether to install an updater program
install-updater = false
# Attach GitHub provenance attestations to release artifacts.
github-attestations = true
